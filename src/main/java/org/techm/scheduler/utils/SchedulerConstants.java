package org.techm.scheduler.utils;


/**
 * 
 * @author Himanshu
 *
 */
public class SchedulerConstants {
	
	/** Name of the media type for Job. */
	public static final String JOB_MIME = "scheduler/job.mime";
	
	/** Name of the media type for Trigger. */
	public static final String TRIGGER_MIME = "scheduler/trigger.mime";
	
	/** Name of the media type for Config. */
	public static final String CONFIG_MIME = "scheduler/config.mime";
	
	/** Name of the JSON property carrying the resource id. */
    public static final String ID_PROP = "id";

    /** Name of the JSON property carrying the resource name. */
    public static final String NAME_PROP = "name";

    /** Name of the JSON property carrying the resource description. */
    public static final String DESCRIPTION_PROP = "description";

    /** Name of the JSON property carrying the name's language. */
    public static final String SET_LANGUAGE_PROP = "lang";

    /** Name of the JSON property carrying the name's value. */
    public static final String SET_TEXT_PROP = "text";

    /** Name of the JSON property carrying the resource job data attributes. */
    public static final String JOB_DATA_ATTRIBUTES_PROP = "jobDataAttributes";

    /** Name of the JSON property carrying the resource value. */
    public static final String VALUE_PROP = "value";

    /** Name of the JSON property carrying the resource job action. */
    public static final String JOB_ACTION_PROP = "jobAction";

    /** Name of the JSON property carrying the resource action type. */
    public static final String ACTION_TYPE_PROP = "actionType";

    /** Name of the JSON property carrying the resource device id. */
    public static final String DEVICE_ID_PROP = "deviceId";

    /** Name of the JSON property carrying the resource command id. */
    public static final String COMMAND_ID_PROP = "commandId";

    /** Name of the JSON property carrying the resource event id. */
    public static final String EVENT_ID_PROP = "eventId";

    /** Name of the JSON property carrying the resource message. */
    public static final String MESSAGE_PROP = "message";

    /** Name of the JSON property carrying the resource group id. */
    public static final String GROUP_ID_PROP = "groupId";

    /** Name of the JSON property carrying the resource stream id. */
    public static final String STREAM_ID_PROP = "streamId";

    /** Name of the JSON property carrying the resource stream id. */
    public static final String RULE_TRIGGER_ID_PROP = "ruleTriggerId";

    /** Name of the JSON property carrying the resource scheduler type. */
    public static final String SCHEDULER_TYPE_PROP = "schedulerType";

    /** Name of the JSON property carrying the resource simple scheduler attributes. */
    public static final String SIMPLE_SCHEDULER_ATTRIBUTES_PROP = "simpleSchedulerAttributes";

    /** Name of the JSON property carrying the resource cron scheduler attributes. */
    public static final String CRON_SCHEDULER_ATTRIBUTES_PROP = "cronSchedulerAttributes";

    /** Name of the JSON property carrying the resource job key. */
    public static final String JOB_KEY_PROP = "jobKey";

    /** Name of the JSON property carrying the resource trigger key. */
    public static final String TRIGGER_KEY_PROP = "triggerKey";

    /** Name of the JSON property carrying the resource group. */
    public static final String GROUP_PROP = "group";

    /** Name of the JSON property carrying the city resource of config. */
    public static final String CITY_ID = "cityId";

    /** Name of the JSON property carrying the user id resource of sync config. */
    public static final String USER_ID = "user_id";

    /** Name of the JSON property carrying the sun rise sun set URL resource of sync config. */
    public static final String SYNC_TIME = "sync_time";

    /** Name of the JSON property carrying the sync time unit resource of sync config. */
    public static final String SYNC_TIME_UNIT = "sync_time_unit";

    /** Name of the JSON property carrying the sync time resource of sync config. */
    public static final String SUNSET_SUNRISE_URL = "sunset_sunrise_url";

    /** Name of the JSON property carrying the locality resource of config. */
    public static final String LOCALITY_ID = "localityId";

    /** Name of the JSON property carrying the controller resource of config. */
    public static final String CONTROLLER_ID = "controllerId";

    /** Name of the JSON property carrying the phase resource of config. */
    public static final String PHASE = "phase";

    /** Name of the JSON property carrying the date resource of config. */
    public static final String DATE = "date";

    /** Name of the JSON property carrying the onTime resource of config. */
    public static final String ON_TIME = "onTime";

    /** Name of the JSON property carrying the offTime resource of config. */
    public static final String OFF_TIME = "offTime";

    /** Name of the JSON property carrying the jobId resource of config. */
    public static final String JOB_ID = "jobId";

    /** Name of the JSON property carrying the triggerId resource of config. */
    public static final String TRIGGER_ID = "triggerId";
    
    /** Name of the JSON property carrying the configType resource of config. */
    public static final String CONFIG_TYPE = "configType";

    /** Name of the JSON property carrying the configState resource of config. */
    public static final String CONFIG_STATE = "configState";

    /** Name of the JSON property carrying the isDim resource of config. */
    public static final String IS_DIM = "isDim";
    
    /** Name of the JSON property carrying the weekly days. */
    public static final String DAYS = "days";
    
    /** Name of the JSON property carrying the intensity resource of config. */
    public static final String INTENSITY = "intensity";

    /** Name of the JSON property carrying the dimType resource of config. */
    public static final String DIM_TYPE = "dimType";
    
    /** Name of the JSON property carrying the intensity resource of config. */
    public static final String LAST_EXECUTION = "lastExecutionTime";

    /** Name of the JSON property carrying the dimType resource of config. */
    public static final String NEXT_EXECUTION = "nextExecutionTime";
}
